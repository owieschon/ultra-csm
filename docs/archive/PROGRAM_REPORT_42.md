# Program Report 42 — Architecture cleanup (Harvest Wave F2, Stream 25)

Worktree: `~/dev/ultra-csm-worktrees/architecture-cleanup`, branch `codex/architecture-cleanup`,
off `origin/main` @ `96bda58` (Harvest 17, PR #50) — created explicitly off `origin/main`, not
the shared `~/dev/ultra-csm` main checkout (which had unrelated uncommitted work and a local
`main` diverged from `origin/main` at dispatch start, per instruction). Dispatch:
`~/ultra-csm-dispatches/harvest/25_ARCHITECTURE_CLEANUP.md`, generated by `/megaprompt` kernel
v1.1, profile v1.1, 2026-07-05.

**Tripwires (K12): one flagged.** Diff-budget file count: 14 files vs. the dispatch's stated 10;
line count 185 insertions (within the 250-line ceiling) + 479 deletions (mostly clean dead-code
removal). See IF/THEN #6 below. Not a STOP condition — no named STOP condition covers a
file-count overage, and dropping an owned, ratified sub-item to force-fit the number would be a
worse violation than naming the overage.

## DoD evidence table

| Check | Command | Expected | Observed | Result |
| --- | --- | --- | --- | --- |
| no foreign residue | `grep -rn "REQUIREMENTS amendment\|The Commons" src/` | (no match) | exit 1, no match (`agent_context.py`, the sole carrier, was removed) | PASS |
| pcs. prefix gone | `grep -n '"pcs\.' src/ultra_csm/agent1/slot_b.py` | (no match) | exit 1, no match | PASS |
| relationship_signals resolved one way | `grep -A2 "relationship_signals" docs/PROGRAM_REPORT_42.md` | one of (a)/(b)/(c) named with a receipt | **outcome (b)** — see below | PASS |
| one credential loader | `grep -rn "ultra-csm-live-creds" src/` | single definition site | `connector_catalog.py`'s `DEFAULT_LIVE_CREDS_PATH` (definition) + 1 docstring-prose mention in `notion_reader.py` (not a duplicate parse) | PASS |
| __init__.py exists | `test -f src/ultra_csm/__init__.py && echo yes` | `yes` | `yes` | PASS |
| zero-drift full suite | `LC_ALL=en_US.UTF-8 LANG=en_US.UTF-8 make eval` | passing count == baseline (minus deliberately-removed tests, plus new tests, both named) | baseline `668 passed, 1 skipped` → post-change `682 passed, 1 skipped` — reconciles exactly: `668 − 15 (my deliberate removals: 6 from `test_agent_context.py`, 9 from `test_surveys.py`) + 29 (new tests pulled in by the Phase-4 rebase from 2 upstream commits that merged during this dispatch's run, unrelated to my changes: `ae4cfa6`/`b3d3421`) = 682` | PASS |
| lint clean | `make lint` | `All checks passed!` | `All checks passed!` (ran as `ruff check src eval tests scripts`, the Makefile's literal `lint` target) | PASS |
| diff budget held | `git diff origin/main... --stat \| tail -1` | ≤10 files, ≤250 lines | 14 files changed, 185 insertions(+), 479 deletions(-) | FILE COUNT OVER by 4; insertion-line budget held (185 of 250) |

## IF/THEN branches taken

1. **`agent_context.py`/`surveys.py` — dead-code disposition (delete vs. wire).** Both had zero
   non-test callers, confirmed twice (Precondition grep, then a fresh Phase 0 re-derivation).
   IF a documented-but-unbuilt future consumer exists for a candidate (found: `docs/
   EXECUTOR_HANDOFF_LANE_E.md` names a future `survey_detractor` factor consuming
   `SurveyResponse`, alongside three other equally-unbuilt future factors —
   `trajectory_decline`, `champion_fragility`, `engagement_collapse`), THEN that does not count
   as "a clean, in-budget wiring opportunity" under this dispatch's own Decisions bar — it's
   planning documentation for a later, separate dispatch (Lane E), not a same-dispatch wiring
   target. Chose: removed both modules with their tests, per Decisions' own explicit fallback
   ("removing dead weight is not the same as removing live behavior").
2. **`live_email_thread` — kept as reference implementation, not deleted.** Confirmed via fresh
   grep that 8 other `data_plane` comms-fixture modules cite this function BY NAME in their own
   docstrings as "the live-read equivalent pattern," exactly as Decisions anticipated. IF a
   function's entire purpose is to be the pattern other modules match against (not to have a
   sweep-path caller of its own), THEN deleting it would break 8 files' documentation
   references for no benefit. Chose: kept it, added a docstring paragraph naming its 8 citing
   modules + its own test explicitly, per Decisions' suggested resolution — no cleaner wiring
   opportunity was found that would beat leaving it as-is.
3. **`pcs.` → rename target: `ultra_csm.` chosen over `ucsm.`.** Decisions said "use whichever
   [prefix] is already established... or `ucsm.` if neither is" but only instructed checking
   "in this file" (`slot_b.py`, which has neither). IF a repo-wide check (not just
   file-local) finds an established convention Decisions' text didn't anticipate looking for,
   THEN honoring that established convention serves the instruction's actual intent
   (consistency) better than defaulting to `ucsm.` on a technicality of where the check was
   scoped. Found: `data_plane/explorer.py` and `data_plane/source_mapping.py` already use
   `"ultra_csm.tenant.id"`, `"ultra_csm.account.id"`, `"ultra_csm.metric.name"`, etc.
   repo-wide; zero `"ucsm."` usage exists anywhere. Chose: renamed to `ultra_csm.llm.tokens` /
   `ultra_csm.llm.cost_usd` / `ultra_csm.llm.latency_ms`. Also found and fixed
   `tests/test_agent1_slot_b.py:360`'s literal-string assertion of the old prefix — not named
   in the dispatch's own gate list, would have silently broken `make eval` if missed.
4. **`relationship_signals.py`/`value_model.py` import chain — outcome (b), with a receipt for
   why the dispatch's own two prior descriptions (shipcheck vs. mid-emission recheck)
   disagreed.** Fresh, from-scratch derivation at Phase 0 (ignoring both the shipcheck's
   original claim and the dispatch's own "recheck" claim, per the Mission's explicit
   instruction): read `value_model.py` in full — line 27 (before my fix) was
   `from ultra_csm.data_plane.relationship_signals import JobChangeSignal`, consumed by
   `_champion_departed_factor`, wired into `build_customer_value_model` whenever `as_of` is
   provided. Traced the full production call chain: `agent1/sweep.py::_person_layer_inputs`
   (own docstring: "Additive person-layer fetch (Harvest 16)") → `data_plane/fixtures.py`'s
   `FixtureCRMDataConnector.list_job_changes` → threaded into
   `build_customer_value_model(..., job_changes=job_changes, ...)`. `_api_helpers.py` (the
   API-facing path) threads the same value the same way. Pinned the receipt: `git log -p
   --follow -- src/ultra_csm/value_model.py` shows the `JobChangeSignal` import added in commit
   `062a3be` — "Harvest 16: Wire the dormant person layer into the live sweep (#48)" — which is
   on `origin/main`, one commit before my worktree's base (`96bda58` = Harvest 17, the very
   next commit). IF the shipcheck's original finding and the dispatch's own same-day
   "mid-emission recheck" disagree, THEN the recheck was almost certainly run against a
   pre-Harvest-16 checkout — i.e., exactly the "temporarily-inconsistent local checkout" hazard
   the Mission itself named as a live possibility, borne out empirically rather than assumed.
   `relationship_signals.py`'s own docstring ("Dormant... no code path does yet") was also
   stale, predating Harvest 16, never updated when the wiring landed. Checked and ruled out
   outcome (c) first (`grep -n "JobChangeSignal" src/ultra_csm/data_plane/contracts.py` →
   not present before my edit — no other in-flight work had already moved it). ACTION: moved
   `JobChangeSignal` (dataclass) + `JobChangeType` (Literal) from `relationship_signals.py`
   into `data_plane/contracts.py`, placed immediately after its sibling person-record type
   `StakeholderRelationship` (contracts.py had, and still has, zero fixture-module imports —
   verified clean host both before and after). `value_model.py` now imports `JobChangeSignal`
   from `contracts.py` alongside its 10 other contract imports and no longer mentions
   `relationship_signals` anywhere in its own source (`grep -n "relationship_signals"
   src/ultra_csm/value_model.py` → empty). `relationship_signals.py` re-exports the type via
   `__all__` + a plain import, so `person_factors.py`'s existing import keeps working
   unchanged (identity-verified: all four modules resolve to the exact same class object).
   `relationship_signals.py`'s stale docstring rewritten to describe the real Harvest-16
   consumption path. IF `person_factors.py` (outside this dispatch's OWNS list) could also be
   pointed directly at `contracts.py` for full consistency, THEN left it importing via the
   re-export instead — it's outside OWNS, it isn't broken, and the Ownership map's own
   instruction is "work around additively; never edit" for out-of-scope files that aren't
   gapped. Not recorded as an Owner Ask since nothing is actually broken, just one hop of
   harmless indirection.
5. **Credential-loader consolidation — `notion_reader.py`'s connector-specific path-override
   preserved, not collapsed.** `notion_reader.py` had its own `ULTRA_CSM_NOTION_CREDS_PATH`
   override; the new shared loader's generic `ULTRA_CSM_LIVE_CREDS_PATH` doesn't provide that
   same per-connector granularity. IF fully collapsing to only the generic override would
   silently drop an existing operational capability, THEN (per K2, additive over destructive at
   an ambiguous fork) layer the connector-specific override ON TOP of the shared loader instead
   of removing it. Chose: `notion_reader.py`'s resolution order is now explicit `creds_path` arg
   > `ULTRA_CSM_NOTION_CREDS_PATH` > shared loader's own `ULTRA_CSM_LIVE_CREDS_PATH`/default —
   still achieves Decisions' actual ask (ONE parse implementation, not two) without regressing
   Notion's existing override granularity. Separately found and fixed a dangling reference to
   the old `_DEFAULT_CREDS_PATH` module constant (removed in the same edit) inside
   `live_authoring_payload`'s `NotionReadError` message, which would have raised `NameError` at
   the exact moment credentials are missing — the one path most likely to be hit in practice.
6. **Diff budget: 14 files vs. stated 10 (final count).** Phase 1 alone (5 sub-items resolved
   as scoped) already reaches exactly 10 files. Phases 2-3's explicitly-named scope
   (credential-loader consolidation touching `connector_catalog.py`/`notion_reader.py`;
   packaging touching a new `src/ultra_csm/__init__.py`) structurally requires 4 more distinct
   files no matter how tightly each individual edit is scoped. The Sanctioned Exceptions
   section names exactly ONE bounded escape valve (the packaging item may scope down to
   documentation-only) and does not name a file-count exception. IF the file-count ceiling and
   the Mission's explicit five-independent-item scope cannot both be satisfied, THEN honor the
   five-item scope (the Mission/Decisions/Phases sections are more specific and more central to
   this dispatch's own stated done-sentence than a generic top-of-document tripwire number that
   reads like an unadjusted template default for a 5-independent-item dispatch of this shape) —
   under-delivering against a ratified Decisions section to satisfy a budget number would itself
   be a worse K1 violation than naming the overage honestly. This same pattern (a docs-only
   sibling dispatch, Stream 22/#51, hitting the identical class of tension — its own mandatory
   report file pushing a clean 11-file/10-file-authorized diff to 12) is independently visible
   in `docs/PROGRAM_REPORT_39.md`'s own IF/THEN #6, suggesting this is a recurring
   template-sizing issue across this wave's dispatches, not specific to this one. Recorded as
   an Owner Ask below rather than silently absorbed or fixed by dropping scope.
7. **`pyproject.toml` sequencing (Stream 19) — turned out to be moot for this dispatch's actual
   diff.** Per instruction, checked `origin/main` and `gh pr list` before opening this PR:
   Stream 19's PR (#55, "Harvest 19: License + Python 3.10 floor (Report 36)") is confirmed
   still OPEN, not merged (`origin/main`'s current `pyproject.toml` has no `license` field).
   IF the packaging fix's config/knowledge disposition ends up scoped to
   documentation-only (per IF/THEN #8 below) rather than real package-data, THEN it requires
   zero `pyproject.toml` edits at all — the elaborate sequence-after-Stream-19 instruction
   turns out not to apply to this dispatch's actual final diff, because there is nothing on my
   side to conflict with Stream 19's `license = "MIT"` addition. Confirmed via
   `git diff origin/main...HEAD -- pyproject.toml` → empty. Stated plainly rather than
   hedging, since the sequencing instruction's premise (both dispatches touch the same lines)
   did not materialize once the Sanctioned Exception was invoked. **No rebase-after-Stream-19
   action is needed for this dispatch specifically** (unlike a scenario where I had actually
   edited `pyproject.toml`) — noted as an Owner Ask anyway per the caller's instruction, in case
   the owner later asks a future dispatch to implement the real package-data fix, which WOULD
   then touch `pyproject.toml` and would need the sequencing check redone at that time.
8. **Packaging fix — Sanctioned Exception invoked for config/knowledge/migrations.** Surveyed
   every `Path(__file__).resolve().parents[N]` repo-root anchor
   (`grep -rn "parents\[2\]\|parents\[3\]\|REPO = Path"`): the pattern recurs in ~9 production
   modules beyond `value_model.py` (`api.py`, `mcp_server.py`, `cli.py`, `knowledge.py`,
   `triggers.py`, `tick.py`, 4 `agent1/*.py` files). IF a real package-data fix requires
   rewriting all ~9 files' resolution logic to `importlib.resources`-style access (package-data
   declarations alone don't fix a non-editable install's runtime resolution — they only control
   wheel contents, not how already-installed code finds files after `site-packages`
   relocation), THEN that rewrite is larger than this dispatch's remaining budget stacked on 4
   other independent items. Chose: invoked the Sanctioned Exception, scoped down to a
   documented constraint (in `value_model.py`, the exact anchoring line Decisions names, and
   in `README.md`'s install section) rather than a real fix. Verified the constraint
   empirically, not just asserted: built a non-editable install into a clean scratch venv and
   ran the Routing table's own literal oracle command
   (`python -c "from ultra_csm.value_model import load_value_model_config;
   load_value_model_config()"`) — reproduced a real `FileNotFoundError` for the missing
   `config/value_model_config.json`, confirming the documented failure mode is observed, not
   hypothetical.

## Owner Asks

1. **`relationship_signals.py`'s docstring/module boundary reflects a Harvest-16-era decision
   that has since shifted** — worth a sentence of institutional memory: report 12 originally
   placed `JobChangeSignal` in its own module explicitly "NOT added to `contracts.py`" (a
   deliberate choice at the time, likely because the type was still dormant/speculative then).
   Harvest 16 (#48) wired it into live production scoring without revisiting that placement
   decision, which is what created the deterministic-core → fixture-content import chain this
   dispatch fixes. No action needed now (fixed in this dispatch), but worth flagging the
   pattern for future "dormant type gets wired live later" cases: re-examine the module-home
   decision at wiring time, not just at authoring time.
2. **Diff-budget file-count ceiling (10 files) may need resizing for future
   multi-independent-item dispatches of this shape.** This dispatch's Mission explicitly named
   5 independent sub-items; satisfying all 5 as scoped structurally requires 14 files (10 for
   item set 1, +3 for credential consolidation, +3 for packaging, with `live_gmail_reader.py`
   shared across two of those). A sibling dispatch this same wave (Stream 22/#51,
   `docs/PROGRAM_REPORT_39.md`) hit the same class of overage (12 vs. 10) for an unrelated
   reason (its own mandatory report file). Suggest either scaling the file-count budget to the
   number of named independent sub-items, or explicitly exempting the mandatory Phase 4 report
   commit from its own budget accounting in future dispatch templates.
3. **If/when Stream 19's PR (#55) merges, no rebase action is needed on this dispatch's side**
   — confirmed via `git diff origin/main...HEAD -- pyproject.toml` (empty) that this dispatch
   makes zero `pyproject.toml` edits, because the packaging fix was scoped to
   documentation-only per the Sanctioned Exception. This Owner Ask exists only so a future
   package-data-implementing dispatch (if the owner commissions one) knows to re-run the
   Stream-19-merge-status check at that time, since THAT future dispatch's `pyproject.toml`
   edits (real package-data declarations) would actually collide with Stream 19's `license`
   field addition.
4. **Sampled human review recommended on `live_email_thread`'s and `relationship_signals.py`'s
   dispositions specifically**, per the Routing table's own residual note (TASTE-routed
   judgment calls that a mechanical gate can verify the outcome-shape of but not the
   judgment-quality of) — these two carry the dispatch's most non-obvious histories (an
   8-file-cited reference implementation with no caller of its own; an import-chain finding
   that visibly shifted between the shipcheck and the dispatch's own emission).

## STOP conditions hit

None. All three named STOP conditions were checked and did not apply:
- No file this dispatch owns showed content mismatching either of the dispatch's two
  descriptions (original shipcheck OR mid-emission recheck) in a way suggesting unrelated
  concurrent modification — the ONE place the two descriptions actually disagreed
  (`relationship_signals.py`/`value_model.py`'s import chain) was resolved via fresh
  derivation per the Mission's own instruction, not treated as a STOP-worthy anomaly, because
  the Mission explicitly anticipated and pre-authorized this exact situation ("re-derive the
  actual state yourself... trust neither prior claim").
- `relationship_signals.py`, once freshly checked, was NOT already resolved by other in-flight
  work (outcome (c) explicitly ruled out via `grep -n "JobChangeSignal"
  src/ultra_csm/data_plane/contracts.py` returning empty before my edit).
- `notion_reader.py` did NOT show signs of substantial unrelated in-progress work when touched
  for credential consolidation — its own docstring (dated "accessed 2026-07-05," i.e., the same
  day) explicitly documented itself as already mirroring `live_gmail_reader.py`'s pattern
  deliberately, its last 2 commits (#43, #45) were both clean/complete/non-draft, and
  `git status --short` was clean throughout. Proceeded to edit it per Ownership map's
  "credential-loading section ONLY" scope.

## Skeptical Reviewer paragraph

The claim most worth doubting on a first read is the `relationship_signals.py` outcome (b)
verdict itself, precisely because this dispatch's own text pre-emptively offered a
more-convenient-sounding alternative (outcome (a), "genuinely unreferenced," which would have
meant less work — just a docstring fix). The verdict here rests on tracing an actual production
call chain (`agent1/sweep.py::_person_layer_inputs` → `data_plane/fixtures.py::list_job_changes`
→ `build_customer_value_model(job_changes=...)`) and pinning it to a specific, already-merged
commit (`062a3be`, Harvest 16/#48) via `git log -p --follow`, rather than trusting either of the
dispatch's own two prior descriptions — both of which turned out to be describing different
moments in this repo's history (pre- vs. post-Harvest-16), neither fabricated, just
time-inconsistent. The weakest part of this report is the credential-loader consolidation's
*design* quality (TASTE-routed per the Routing table, not LOOPABLE) — the mechanical gates prove
one parse implementation exists and the relevant tests pass, but they cannot prove the
`ULTRA_CSM_NOTION_CREDS_PATH`-layered-on-`ULTRA_CSM_LIVE_CREDS_PATH` resolution order is the
cleanest possible design rather than merely a safe, additive one; a reviewer preferring a fully
collapsed single-env-var scheme (accepting the loss of Notion's per-connector override) would
have a legitimate, different-but-defensible alternative. The diff-budget overage (14 vs. 10
files) is disclosed rather than hidden, but a skeptical reader should independently verify the
claim that no STOP condition applies to a budget overage — re-reading the dispatch's own STOP
conditions section confirms none of its three named conditions mention diff size, so this
report's judgment call rests on that section being exhaustive, which is itself worth the reader
double-checking against the dispatch file directly rather than taking this report's word for it.

## Final verification table

| Item | Verified how | Result |
| --- | --- | --- |
| origin/main synced before worktree creation | `git fetch origin --quiet && git log origin/main -1 --oneline` | `96bda58`, then re-verified again at Phase 4 (`b3d3421`, 2 commits later — rebased cleanly) |
| Worktree correctly branched off origin/main, not local main | `git worktree add ~/dev/ultra-csm-worktrees/architecture-cleanup -b codex/architecture-cleanup origin/main` | HEAD=`96bda58`, clean; confirmed local `main` (`0b66e2d`) had diverged from `origin/main` before starting, exactly as the caller warned |
| `relationship_signals.py`/`value_model.py` import chain | fresh `grep`, `git log -p --follow`, production-call-chain trace via `sweep.py`/`fixtures.py`/`_api_helpers.py`, identity check across all 4 consuming modules | outcome (b), receipt = commit `062a3be` |
| Dead-code removals (`agent_context.py`, `surveys.py`) | fresh caller-count grep before removal; full test suite after | 0 non-test callers each; removed cleanly with their tests |
| `live_email_thread` disposition | fresh grep of all 8 citing modules' docstrings | kept, docstring clarified |
| `pcs.` → `ultra_csm.` rename | repo-wide grep for both `"ucsm."` and `"ultra_csm."` conventions before choosing | `ultra_csm.` established elsewhere (`explorer.py`, `source_mapping.py`), `ucsm.` used nowhere |
| Credential-loader consolidation | grep for remaining inline parses; targeted + broader pytest run; import-identity check | 1 definition site; 6+5 tests pass; identity confirmed across 3 modules |
| `__init__.py` packaging fix | `test -f` gate; import check confirms `__file__` resolves to the new file | `yes`; confirmed |
| Non-editable-install constraint | empirical: built a non-editable install in a clean scratch venv, ran the Routing table's literal oracle command | reproduced real `FileNotFoundError`, receipt below |
| `pyproject.toml` Stream-19 sequencing | checked `origin/main`'s current `pyproject.toml` (no `license` field) + `gh pr list` (PR #55 OPEN) + own diff (`git diff origin/main...HEAD -- pyproject.toml` empty) | Stream 19 not yet merged; moot for this dispatch — zero `pyproject.toml` edits on my side |
| Rebase onto current `origin/main` at Phase 4 | `git fetch && git rebase origin/main` | clean, no conflicts (2 new upstream commits, no line-range overlap with my changes) |
| `make lint` | `ruff check src eval tests scripts` (the Makefile's literal `lint` target) | `All checks passed!` |
| Zero-drift full suite | `LC_ALL=en_US.UTF-8 LANG=en_US.UTF-8 make eval` | `668 passed` (baseline) → `682 passed` (post-change), reconciles exactly to `−15 my removals +29 rebased-in upstream tests` |
| Diff budget | `git diff origin/main... --stat` | 14 files / 185 insertions(+) / 479 deletions(-) — file count over by 4, insertion-line budget held |

## Receipts appendix

**Worktree:** `~/dev/ultra-csm-worktrees/architecture-cleanup`, branch `codex/architecture-cleanup`,
based on `origin/main` @ `96bda58` (Harvest 17, PR #50), rebased cleanly onto `origin/main` @
`b3d3421` at Phase 4.

**Commits (3, one per phase 1-3; this report is Phase 4's commit):**
```
b9b153e refactor: resolve dead-code/residue/import-chain architecture findings (see report for per-item disposition)
46c03b9 refactor: consolidate three credential-loading conventions into one shared loader
76395cc fix: add top-level package __init__.py; address non-editable-install config/knowledge gap
```

**Baseline (Phase 0, before any file touched):**
`LC_ALL=en_US.UTF-8 LANG=en_US.UTF-8 make eval 2>&1 | tail -5` →
`668 passed, 1 skipped, 1 warning in 636.52s (0:10:36)` (the 1 warning is an unrelated
`StarletteDeprecationWarning`, not a failure).

**Post-change full suite (Phase 4, after rebase onto current origin/main):**
`LC_ALL=en_US.UTF-8 LANG=en_US.UTF-8 make eval 2>&1 | tail -5` →
`682 passed, 1 skipped, 1 warning in 404.82s (0:06:44)` (same unrelated
`StarletteDeprecationWarning` as baseline).

**Exact reconciliation (668 → 682, verified via git history, not estimated):**
- My deliberate removals: `git show 96bda58:tests/test_agent_context.py | grep -c "^def test_"`
  → 6; `git show 96bda58:tests/test_surveys.py | grep -c "^def test_"` → 9. Total removed: 15.
- Rebased-in additions (2 upstream commits — `ae4cfa6` "Stream 22: Public docs integrity" and
  `b3d3421` "Content Roadmap..." — merged to `origin/main` during this dispatch's run,
  independent of my changes, pulled in by the Phase 4 rebase): 4 new test files
  (`test_content_roadmap.py`: 3, `test_content_roadmap_push.py`: 5,
  `test_content_route_matcher.py`: 9, `test_gmail_writeback.py`: 8 = 25) plus 4 new tests added
  to the pre-existing `test_notion_render.py` (5 → 9 test functions, `git show
  96bda58:tests/test_notion_render.py | grep -c "^def test_"` vs. current). Total added: 29.
- `668 − 15 + 29 = 682` — exact match to the observed post-change count. Zero unexplained
  drift.

**Files touched (14):**
```
README.md                                        |   7 +
src/ultra_csm/__init__.py                        |   3 +
src/ultra_csm/agent1/slot_b.py                   |   6 +-
src/ultra_csm/agent_context.py                   |  70 --------
src/ultra_csm/data_plane/connector_catalog.py    |  48 ++++++
src/ultra_csm/data_plane/contracts.py            |  32 ++++
src/ultra_csm/data_plane/live_gmail_reader.py    |  37 +++--
src/ultra_csm/data_plane/notion_reader.py        |  50 +++---
src/ultra_csm/data_plane/relationship_signals.py |  54 +++---
src/ultra_csm/data_plane/surveys.py              | 202 -----------------------
src/ultra_csm/value_model.py                     |  20 ++-
tests/test_agent1_slot_b.py                      |   2 +-
tests/test_agent_context.py                      |  63 -------
tests/test_surveys.py                            |  70 --------
14 files changed, 185 insertions(+), 479 deletions(-)
```

**`relationship_signals.py` import-chain receipt:** `git log -p --follow -- src/ultra_csm/
value_model.py` shows `from ultra_csm.data_plane.relationship_signals import JobChangeSignal`
added in commit `062a3be` ("Harvest 16: Wire the dormant person layer into the live sweep
(#48)"), already on `origin/main` (one commit before this worktree's base commit, `96bda58`).

**Non-editable-install empirical receipt:** clean scratch venv, `pip install
/path/to/worktree` (no `-e`), then `python -c "from ultra_csm.value_model import
load_value_model_config; load_value_model_config()"` →
```
FileNotFoundError: [Errno 2] No such file or directory:
'.../site-packages/config/value_model_config.json'
```
Scratch venv discarded after capturing the receipt.

**Stream 19 status check (at Phase 4, before opening this PR):** `gh pr list` → PR #55
("Harvest 19: License + Python 3.10 floor (Report 36)") state `OPEN`. `origin/main`'s current
`pyproject.toml` has no `license` field. `git diff origin/main...HEAD -- pyproject.toml` on
this branch → empty (zero edits). No rebase-after-merge action needed for this dispatch.

**Lint:** `ruff check src eval tests scripts` → `All checks passed!` (run repo-wide, not just
touched files).

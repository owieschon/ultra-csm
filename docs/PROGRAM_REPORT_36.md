# Program Report 36 — License + Python 3.10 Floor (Harvest Wave F2)

Branch `codex/license-and-py310-floor` off synced `main` (`origin/main` at
`96bda58`, verified diverged from the shared checkout's local `main` at
dispatch start — worktree branched from `origin/main` specifically, per
instruction). Dispatch: `~/ultra-csm-dispatches/harvest/19_LICENSE_AND_PY310_FLOOR.md`,
generated by `/megaprompt` kernel v1.1, profile v1.1, 2026-07-05.

**Tripwires: 0 crossed.** No IF/THEN tripwire threshold (8) reached. The
ownership map's code-diff budget (4 files / 60 lines) held cleanly: the
four files it authorizes (`LICENSE`, `pyproject.toml`, `ui/package.json`,
`eval/external_corpus_probe.py`) total 4 files changed, 31 lines
(27 insertions + 4 deletions) — see Final verification table. Naming the
excess per Phase 3's instruction rather than silently absorbing it: the
raw `git diff origin/main... --stat` measured at final HEAD is 5 files /
197 lines (193 insertions + 4 deletions), because it now includes this
report file itself (166 insertions, growing as the report documents its
own corrections) — the report is Phase 3's own named deliverable
with its own dedicated commit (`docs: report 36...`), not a scope
violation of the ownership map, but its size means the dispatch's stated
"≤4 files, ≤60 lines" DoD-table number is only exactly true for the
code-only diff computed before this file existed, not for the final
repo state including the report. Recorded both numbers explicitly rather
than picking the flattering one.

## DoD Evidence (observed results, verbatim)

| Check | Command | Observed |
| --- | --- | --- |
| LICENSE exists | `test -f LICENSE && echo yes` | `yes` |
| MIT text present | `grep -c "MIT License" LICENSE` | `1` |
| pyproject license field | `python3 -c "import tomllib; print(tomllib.load(open('pyproject.toml','rb'))['project']['license'])"` | `MIT` |
| package.json license field | `python3 -c "import json; print(json.load(open('ui/package.json'))['license'])"` | `MIT` |
| no more datetime.UTC | `grep -rn "from datetime import.*UTC" --include="*.py" . \| grep -v .venv` | (empty output, grep exit 1) |
| eval suite green (Phase 2 gate, post-fix) | `LC_ALL=en_US.UTF-8 LANG=en_US.UTF-8 make eval` | `668 passed, 1 skipped, 1 warning in 570.40s (0:09:30)`, exit 0 |
| lint clean | `make lint` | `All checks passed!` |
| hygiene clean | `make hygiene` | exit 0, no output |
| diff budget held | `git diff origin/main... --stat \| tail -1` | `4 files changed, 27 insertions(+), 4 deletions(-)` |
| pip metadata still parses | `pip install -e . --dry-run 2>&1 \| tail -3` | `Would install ultra-csm-0.0.0`, no error |

Baseline (Phase 0, before any edit, on the same `origin/main` HEAD):
`668 passed, 1 skipped, 1 warning in 548.26s (0:09:08)`, exit 0. Post-fix
Phase 2 gate result is identical in pass/skip counts (668/1) — zero new
failures, zero new skips, same pre-existing warning
(`StarletteDeprecationWarning` re `httpx`/`starlette.testclient`, unrelated
to this dispatch). Wall-time difference (548s vs 570s) is machine
contention from ~9 sibling-dispatch worktrees running `make eval`
concurrently on this box during this wave, not a regression.

Every gate is green. No BLOCKED items.

## IF/THEN Branches Taken (K2)

None required. Every ambiguous fork named in the dispatch (report slot 36,
license choice MIT, copyright line, field placement in both metadata
files) was pre-ratified in the Decisions section and matched disk state
exactly on inspection — no fork needed independent resolution beyond what
Decisions already specified. The repo-wide `datetime.UTC` grep (both at
Phase 0 and re-run at Phase 2 start) found exactly the one hit the
dispatch's Decisions/Author-belief tag named
(`eval/external_corpus_probe.py:11`) and zero more — the STOP condition for
"more than 3 additional files" did not trigger, and no additional 3.11+-only
construct was found in that file on a full read (only the cited `UTC` name
appears; no other `datetime` 3.11+ API, no `tomllib` used pre-3.11-only path,
no other version-gated stdlib construct).

## Owner Asks

**1. License choice: MIT (flagged per Decisions — a fast default, not a
silently final one).** This dispatch shipped MIT because every direct
Python dependency (fastapi, uvicorn, mcp, pytest, ruff, httpx) is
MIT/BSD-3, `psycopg[binary]` is LGPL-3.0-only but consumed unvendored (no
distribution obligation triggered), and every direct npm dependency in
`ui/` is MIT/Apache-2.0/BSD. MIT is compatible with all of them and is the
shortest, most common choice for a portfolio/demo repo. **The owner may
prefer Apache-2.0 (explicit patent grant) or a source-available notice
instead** — this dispatch shipped the default rather than leaving the repo
unlicensed while a preference is decided. Swapping license text later is a
single-file, low-risk change if a different license is preferred.

2. No other Owner Asks. The `datetime.UTC` fix and license-field additions
are mechanical and fully verified; nothing else in this dispatch's scope
required owner judgment.

## STOP Conditions Hit

None. All three Preconditions passed cleanly at dispatch start (main
synced and green at `origin/main`=`96bda58`; LICENSE absent; `datetime.UTC`
import present at the cited line) — none of the three STOP conditions
(LICENSE pre-existing, import pre-fixed, or a repo-wide sweep exceeding 3
additional files) fired at any point.

## Skeptical Reviewer Paragraph

What this dispatch does NOT prove: it does not verify runtime behavior on
an actual Python 3.10 interpreter — no `python3.10` (nor `pyenv`/`asdf`
managing one) was available in this worktree's environment (only system
Python 3.14.3, which is what ran every `make eval`/`make lint`/`make
hygiene` gate above). The fix (`from datetime import datetime, timezone`
+ `timezone.utc` in place of the 3.11+-only `datetime.UTC` singleton) is a
well-established, version-safe, semantically-equivalent stdlib API
available in Python 3.2+, so 3.10 compatibility is a strong static-analysis
inference, not an executed proof — the residual the dispatch's own Routing
table anticipated ("cannot prove behavior on an actual Python 3.10
interpreter if none is available in the worktree environment — state
whichever is true rather than assume"). It also does not verify the MIT
license text against a second independent source beyond the author's own
verbatim knowledge of the canonical text (the SPDX/OSI-published MIT
License) — no external fetch was performed to diff against
opensource.org's copy; the text was reproduced from a well-known, unchanging,
widely-published legal document, not synthesized. It does not sweep the
repo for any other 3.10-incompatible construct beyond the one grep pattern
the dispatch named (`from datetime import.*UTC\b`) — a different construct
(e.g., a 3.11+-only `tomllib` behavior difference, `ExceptionGroup`, or
`Self` typing without `from __future__ import annotations`) could exist
undetected; none were observed on a full manual read of the one file this
dispatch was scoped to touch, and this dispatch's ownership map explicitly
excludes a repo-wide sweep for other constructs.

## Final Verification Table (command + verbatim result)

| Command | Verbatim result |
| --- | --- |
| `git -C ~/dev/ultra-csm fetch origin --quiet && git log origin/main -1 --oneline` | `96bda58 Harvest 17: Surface the person layer in the operations UI (#50)` |
| `test -f LICENSE && echo EXISTS \|\| echo ABSENT` (precondition) | `ABSENT` |
| `grep -n "from datetime import UTC" eval/external_corpus_probe.py` (precondition) | `11:from datetime import UTC, datetime` |
| `grep -rn "from datetime import.*UTC\b" --include="*.py" .` (excl .venv/node_modules/.git) | exactly 1 hit: `eval/external_corpus_probe.py:11` |
| `test -f LICENSE && python3 -c "...tomllib...license==MIT" && python3 -c "...json...license==MIT"` (Phase 1 gate) | exit 0, no output |
| `.venv/bin/pip install -e . --dry-run` | `Would install ultra-csm-0.0.0` |
| `grep -n "\bUTC\b" eval/external_corpus_probe.py` (post-fix) | exit 1, no matches |
| `LC_ALL=en_US.UTF-8 LANG=en_US.UTF-8 make eval` (Phase 0 baseline) | `668 passed, 1 skipped, 1 warning in 548.26s (0:09:08)` |
| `LC_ALL=en_US.UTF-8 LANG=en_US.UTF-8 make eval` (Phase 2 gate, post-fix) | `668 passed, 1 skipped, 1 warning in 570.40s (0:09:30)` |
| `make lint` | `All checks passed!` |
| `make hygiene` | exit 0 |
| `git diff origin/main... --stat` (code-only, before report commit) | `LICENSE \| 21 +++++++++++++++++++++`, `eval/external_corpus_probe.py \| 8 ++++----`, `pyproject.toml \| 1 +`, `ui/package.json \| 1 +`, `4 files changed, 27 insertions(+), 4 deletions(-)` |
| `git diff origin/main... --stat` (final, after report commits — self-referential, grows with each report edit; not re-chased to an exact final byte count) | 5 files changed (`LICENSE`, `docs/PROGRAM_REPORT_36.md`, `eval/external_corpus_probe.py`, `pyproject.toml`, `ui/package.json`); report file itself accounts for the great majority of insertions. The stable, decision-relevant number is the code-only diff above (4 files/31 lines) — the report's own size is not part of the ownership-map budget claim. |
| `gh api repos/:owner/:repo --jq .allow_auto_merge` | `true` |
| `gh api repos/:owner/:repo/branches/main/protection` | 200 (does not 404); `required_status_checks.contexts` includes `"eval + CSM scorecard"` |

## Receipts Appendix

- Worktree: `~/dev/ultra-csm-license-and-py310-floor`, branch
  `codex/license-and-py310-floor`, tracks `origin/main` at creation,
  pushed to `origin/codex/license-and-py310-floor`.
- Base SHA: `96bda58ee518d64daf8f76d4bede3ccc65e70e6a` (`origin/main` HEAD
  at dispatch start). Confirmed diverged from the shared checkout's local
  `main` (`0b66e2d...`, 4 commits each direction via
  `git rev-list --left-right --count main...origin/main`) — worktree
  correctly branched from `origin/main`, not local `main`, per explicit
  instruction.
- Commit 1: `04101dc` — `license: add MIT LICENSE + pyproject/package.json
  license fields` (3 files changed, 23 insertions(+)).
- Commit 2: `4c80b30` — `fix: use timezone.utc instead of datetime.UTC
  (Python 3.10 floor)` (1 file changed, 4 insertions(+), 4 deletions(-)).
- Commit 3 (this report): see below.
- Files touched (exactly the ownership map, nothing else):
  `LICENSE` (new), `pyproject.toml` (`license` key only),
  `ui/package.json` (`license` key only),
  `eval/external_corpus_probe.py` (import line + 3 `UTC` usages only).
- `PROGRESS.md` maintained at worktree root throughout, excluded from git
  via the shared `~/dev/ultra-csm/.git/info/exclude` (idempotent — the
  `PROGRESS.md` pattern was already present from sibling dispatches in
  this wave doing the same K3 step; no duplicate line was appended).
- `BLOCKED.md`: not created — no STOP condition fired, nothing to block on.
- PR: left OPEN per this wave's explicit Merge Policy override (see PR
  body) — not run through `gh pr merge --auto` despite
  `allow_auto_merge=true` and branch protection both being configured.

"""Namespace package for Universe v2 tenants added after fleetops
(`docs/UNIVERSE_V2_CONVENTIONS.md`, "Namespacing + live/fixture boundary").
fleetops' own modules stay flat at ``ultra_csm.data_plane.*`` by convention;
this package holds the tenants built after that convention was written:
`fieldstone`, `crateworks`, `loopway`.
"""

from __future__ import annotations
